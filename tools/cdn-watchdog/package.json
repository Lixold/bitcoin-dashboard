{
  "name": "bitcoin-dashboard-cdn-watchdog",
  "version": "0.1.0",
  "private": true,
  "description": "Scheduled checker for the published CDN payloads. Reads what the Workers publish and opens a GitHub issue when a payload goes silent or goes empty. Deliberately has no dependencies: it must be able to run when the rest of the toolchain cannot.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec"
  }
}
