{
  "name": "bitcoin-dashboard-workers",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers for the Bitcoin Dashboard data pipeline. Dependencies declared here are picked up by every Worker under workers/cron-*/ via wrangler's node_modules resolution.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fast-xml-parser": "^5.5.7"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec"
  }
}
